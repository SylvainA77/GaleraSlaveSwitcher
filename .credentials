basecredentials="-umonitor -pmonitor -h"

getcredentials()
{
  [[ -n "$debug" ]] && echoerr "switchover args : $*"
  [ $# -ne 1 ] && echo "getcredentials function requires 1 arg : host ip" && exit -1
   echo "$basecredentials$1"
}

